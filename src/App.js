import React from "react";
import "./App.css";
import Home from "./pages/Home";

function App() {
  return (
    <div className="App">
      <h1>Testando essa bodega monstruosa parceiro do mal </h1>

    {/* Home (the one with the search one) */}

    <Home /> 
    {/* SearchPage (the one with results one) */}

    </div>
  );
}

export default App; 
